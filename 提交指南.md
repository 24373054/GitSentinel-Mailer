# 📦 GitHub提交指南

## ✅ 提交前检查清单

### 1. 文件确认

确保以下文件已创建：
- [x] README.md（精美中文版）
- [x] README-EN.md（精美英文版）
- [x] LICENSE（MIT许可证）
- [x] .gitignore（已有）
- [x] package.json（已有）

### 2. 需要更新的内容

在提交前，请将README中的占位符替换为实际信息：

#### README.md 和 README-EN.md 中需要替换：
```
yourusername → 您的GitHub用户名
```

**需要替换的位置：**
- 所有徽章链接
- Star History链接
- Contributors链接
- 仓库链接

### 3. 敏感信息检查

⚠️ **确保不要提交以下文件：**
- ❌ `.env`（包含GitHub Token和邮箱密码）
- ❌ `github_monitor.db`（数据库文件）
- ❌ `node_modules/`（依赖包）

✅ **这些已在 `.gitignore` 中，但请再次确认！**

---

## 🚀 提交步骤

### 步骤1：初始化Git仓库

```bash
# 如果还没有初始化
git init
```

### 步骤2：添加远程仓库

```bash
# 将YOUR_USERNAME替换为您的GitHub用户名
git remote add origin https://github.com/YOUR_USERNAME/github-monitor-system.git
```

### 步骤3：添加文件

```bash
# 添加所有文件
git add .

# 或者选择性添加（推荐）
git add README.md README-EN.md
git add LICENSE .gitignore
git add package.json package-lock.json
git add server/ client/
git add *.md *.bat *.js
```

### 步骤4：检查状态

```bash
# 查看将要提交的文件
git status

# 确保以下文件NOT在列表中：
# - .env
# - github_monitor.db
# - node_modules/
```

### 步骤5：提交

```bash
# 首次提交
git commit -m "🎉 Initial commit: GitHub Monitor & Email Notification System

✨ Features:
- Real-time GitHub repository monitoring
- 5 beautiful email themes (including Yingzhouji cyber epic style)
- Dynamic monitoring interval (5s-1h adjustable)
- Smart rate limit management
- Multi-project support
- SQLite data persistence

🎨 Email Themes:
- Default (Blue-Purple gradient)
- Yingzhouji (Cyber epic, perfect for game projects)
- Minimal (Black & White)
- Warm (Orange sunshine)
- Fresh (Natural green)

⚙️ Tech Stack:
- Backend: Node.js + Express + SQLite
- Frontend: React 18 + Axios
- Email: Nodemailer with custom themes
- API: GitHub REST API

📚 Documentation:
- Complete Chinese and English README
- Email theme guide
- Dynamic monitoring guide
- API rate limit guide
- Quick start guide

🚀 Ready to use with beautiful UI and smart features!"
```

### 步骤6：推送到GitHub

```bash
# 首次推送
git push -u origin main

# 如果分支是master
git push -u origin master
```

---

## 📝 提交信息模板

### 常用提交格式

```bash
# 新功能
git commit -m "✨ feat: 添加XX功能"

# Bug修复
git commit -m "🐛 fix: 修复XX问题"

# 文档更新
git commit -m "📝 docs: 更新README"

# 样式调整
git commit -m "💄 style: 优化UI样式"

# 代码重构
git commit -m "♻️ refactor: 重构XX模块"

# 性能优化
git commit -m "⚡ perf: 优化XX性能"

# 测试相关
git commit -m "✅ test: 添加XX测试"

# 配置更改
git commit -m "🔧 chore: 更新配置"
```

### Emoji指南

| Emoji | 代码 | 说明 |
|:---:|:---|:---|
| 🎉 | `:tada:` | 初始提交 |
| ✨ | `:sparkles:` | 新功能 |
| 🐛 | `:bug:` | Bug修复 |
| 📝 | `:memo:` | 文档 |
| 🎨 | `:art:` | 改进结构/格式 |
| ⚡ | `:zap:` | 性能提升 |
| 🔥 | `:fire:` | 删除代码/文件 |
| 💄 | `:lipstick:` | UI/样式 |
| ♻️ | `:recycle:` | 重构 |
| 🔧 | `:wrench:` | 配置文件 |

---

## 🎯 创建GitHub仓库

### 在GitHub网站上：

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `github-monitor-system`
   - **Description**: `🔍 Real-time GitHub repository monitoring with beautiful email notifications | 实时GitHub仓库监控，精美邮件通知`
   - **Public** 或 **Private**（根据需要选择）
   - ⚠️ **不要勾选**"Add a README file"（我们已经有了）
   - ⚠️ **不要勾选**"Add .gitignore"（我们已经有了）
   - **License**: 选择 MIT License
3. 点击"Create repository"

### 仓库设置建议：

**Topics（标签）：**
```
github, monitor, email, notification, nodejs, react, express, sqlite, 
realtime-monitoring, email-themes, webhook, automation
```

**About（关于）：**
```
🔍 GitHub repository monitoring system with real-time email notifications. 
Features dynamic intervals, 5 beautiful themes, and smart rate limit management.
```

**Website（网站）：**
```
如果有在线演示，填写URL
```

---

## 📋 提交后的操作

### 1. 添加主题标签

在GitHub仓库页面：
1. 点击页面右侧的"⚙️ Settings"
2. 找到"Topics"
3. 添加相关标签

### 2. 创建Release（可选）

```bash
# 创建标签
git tag -a v1.0.0 -m "🎉 Release v1.0.0

First stable release with complete features:
- GitHub repository monitoring
- 5 email themes
- Dynamic monitoring intervals
- Rate limit management"

# 推送标签
git push origin v1.0.0
```

然后在GitHub上：
1. 进入仓库的"Releases"
2. 点击"Draft a new release"
3. 选择刚才创建的标签
4. 填写Release信息
5. 发布

### 3. 启用GitHub Pages（可选）

如果想展示项目文档：
1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: main / docs
4. Save

### 4. 保护主分支（推荐）

Settings → Branches → Add rule：
- Branch name pattern: `main`
- ✅ Require pull request reviews before merging
- ✅ Require status checks to pass

---

## 🔒 安全检查

### 提交前必须确认：

```bash
# 检查.env是否在gitignore中
cat .gitignore | grep .env

# 确认.env不会被提交
git status --ignored | grep .env

# 检查是否有敏感信息
git log --all --full-history --source -S "2315766973@qq.com"
git log --all --full-history --source -S "gkvoxbeffnxmeaaa"
```

### 如果不小心提交了敏感信息：

```bash
# 从历史中删除文件
git filter-branch --force --index-filter \
"git rm --cached --ignore-unmatch .env" \
--prune-empty --tag-name-filter cat -- --all

# 强制推送（谨慎使用）
git push origin --force --all
```

⚠️ **更好的做法：**
1. 立即撤销GitHub Token
2. 更改邮箱授权码
3. 重新生成新的密钥
4. 更新.env文件

---

## 📊 推荐的仓库结构

```
github-monitor-system/
├── 📄 README.md（精美中文版）
├── 📄 README-EN.md（精美英文版）
├── 📄 LICENSE
├── 📄 .gitignore
├── 📄 package.json
├── 📄 package-lock.json
├── 📁 server/
├── 📁 client/
├── 📁 docs/（可选，额外文档）
└── 📄 各种说明文档.md
```

---

## 🎨 README替换清单

**需要手动替换的内容：**

1. **用户名占位符**（共约30处）
   ```
   yourusername → 您的实际GitHub用户名
   ```

2. **可选添加内容：**
   - 项目Banner图片（可用工具生成）
   - 项目截图/演示GIF
   - 在线演示地址（如果有）

---

## 🚀 快速替换命令（参考）

### 在Linux/Mac上：

```bash
# 替换用户名（请先备份！）
USERNAME="your_actual_username"
sed -i "s/yourusername/$USERNAME/g" README.md
sed -i "s/yourusername/$USERNAME/g" README-EN.md
```

### 在Windows PowerShell上：

```powershell
# 替换用户名
$USERNAME = "your_actual_username"
(Get-Content README.md) -replace 'yourusername', $USERNAME | Set-Content README.md
(Get-Content README-EN.md) -replace 'yourusername', $USERNAME | Set-Content README-EN.md
```

---

## ✅ 提交完成后

恭喜！🎉 您的项目现在看起来像一个成熟的开源项目了！

**下一步建议：**
1. ⭐ 给自己的项目点个Star
2. 📝 完善Wiki页面
3. 🐛 创建Issues模板
4. 🤝 创建PR模板
5. 📖 添加贡献指南
6. 🎨 添加项目截图
7. 🔗 分享到社交媒体

---

**祝您的项目收获满满的Star！** ⭐⭐⭐

